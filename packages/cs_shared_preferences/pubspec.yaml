name: cs_shared_preferences
description: This package manages all local data using shared_preferences.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0


environment:
  sdk: '>=2.17.0 <3.0.0'

dependencies:
  shared_preferences: ^2.0.15
  shared_repository:
    path: ../shared_repository

dev_dependencies: 
  flutter_lints: ^2.0.1
  lints: ^2.0.0
